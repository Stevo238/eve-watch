mss>=9.0.1
Pillow>=10.4.0
dxcam>=0.0.5; sys_platform == "win32"
# dxcam imports cv2 but does not declare it; without this the CI build
# fails at grab() with ModuleNotFoundError instead of using dxcam.
opencv-python>=4.8.0; sys_platform == "win32"
